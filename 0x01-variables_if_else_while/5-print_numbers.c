#include <stdio.h>
/**
 * main - prints integers
 * Description: all int are under 10
 * Return: 0
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);

	printf("\n");
	return (0);
}
